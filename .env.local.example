# Local Private Portal authentication only. Never commit real credentials.
# Keep the JSON on one line. Replace the example code with the existing owner
# access code you intend to enter on localhost.
STATISTICS_ACCESS_CODES='[{"id":"owner","name":"Kui Wu","code":"REPLACE_WITH_OWNER_ACCESS_CODE","role":"owner"}]'

# Generate a unique local-only value with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
STATISTICS_SESSION_SECRET=REPLACE_WITH_A_RANDOM_LOCAL_SECRET
